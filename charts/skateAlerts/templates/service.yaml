apiVersion: v1
kind: Service
metadata:
  name: {{ include "skate.name" . }}-service
  namespace: {{ include "skate.namespace" . }}
  labels:
    app: {{ include "skate.name" . }}
  annotations:
    {{ include "skate.service-annotations" . }}
spec:
  selector:
    app: {{ include "skate.name" . }}
  type: {{ include "skate.service-type" . }}
  ports:
    - name: http
      port: {{ include "skate.service-http-port" . }}
      targetPort: {{ include "skate.service-target-port" . }}
      protocol: TCP
    - name: https
      port: {{ include "skate.service-https-port" . }}
      targetPort: {{ include "skate.service-target-port" . }}
      protocol: TCP
---